#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

main() {
	int n;
	do{
		printf("\nInforme quantos alunos ha na turma: "); scanf("%d" ,&n);
	} while(n <= 0);
	float m[n], n1[n], n2[n], mediag = 0;
	
}
